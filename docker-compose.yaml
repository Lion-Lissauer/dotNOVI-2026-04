services:
  postgres:
    image: postgres:18-alpine
    container_name: postgres
    environment:
      POSTGRES_USER: novi
      POSTGRES_PASSWORD: devops123
      POSTGRES_DB: node
    ports: ["5432:5432"]
    volumes:
      - ./src/db/init.sql:/docker-entrypoint-initdb.d/init.sql
      - postgres_data:/var/lib/postgresql
    restart: unless-stopped

  app:
    build: .
    container_name: app
    ports: ["3000:3000"]
    environment:
      DATABASE_URL: postgres://novi:devops123@postgres:5432/node
    depends_on:
      - postgres
    restart: unless-stopped

volumes:
  postgres_data:
